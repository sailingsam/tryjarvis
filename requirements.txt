# The dependency list lives in pyproject.toml now — it has to, so that
# `pip install mantrin` from PyPI carries it. This file remains for muscle
# memory and CI: installing from it = installing the project, core only.
-e .
