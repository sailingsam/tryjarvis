---
title: Weather
description: Ask Mantrin about the weather — no key required.
---

"Do I need an umbrella?"

Open-Meteo is free and needs no key — this integration works the moment its
block is in `mcp.json` (via `uvx`, so it needs
[uv](https://docs.astral.sh/uv/) installed). No setup beyond that.
