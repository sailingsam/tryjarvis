# Voice dependencies live in pyproject.toml extras now: [voice] is the mic
# layer (endpointing, wake word, MP3 decode), [whisper] and [piper] are the
# heavy local providers — `mantrin setup` offers exactly what your choices
# need. This file remains for muscle memory: it installs the lot.
-e .[all]
